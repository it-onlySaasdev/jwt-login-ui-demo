# jwt-login-ui-demo
Secure authentication system built with React and Node.js/Express using JWT tokens. It includes a fast api, mobile-responsive login UI, protected routes, encrypted passwords (bcrypt), form validation, and clean API structure. 
